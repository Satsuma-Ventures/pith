{
  "name": "pith",
  "version": "1.0.0",
  "private": true,
  "description": "Pith — Satsuma Ventures thought-leadership publication. Static site built from published markdown.",
  "scripts": {
    "build": "node build.js",
    "serve": "node build.js && npx --yes serve dist -l 8080"
  },
  "dependencies": {
    "marked": "^12.0.0"
  }
}
